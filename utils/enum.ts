export enum Role {
    MANAGER = 'MANAGER',
    EMPLOYEE = 'EMPLOYEE',
    CUSTOMER = 'CUSTOMER',
}

export enum OrderStatus {
    PENDING = 'PENDING',
    CONFIRMED = 'CONFIRMED',
    CANCELLED = 'CANCELLED',
    DONE = 'DONE',
}
